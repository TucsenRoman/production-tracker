# Canonical proof

`TasksScreen.jsx` is the original source this DNA was measured from
(Protrack, `app/screens/TasksScreen.jsx`, captured 2026-09-03). It's the
worked example other task-list screens should match — not a template to
copy verbatim (it's wired to this app's own state/handlers/domain
model), but the reference for "does a new screen actually look like
this one."
